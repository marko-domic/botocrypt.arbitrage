# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# ----     gRPC services    ----
->        /                  com.botocrypt.arbitrage.routers.ArbitrageRouter
# ---- end of gRPC services ----

# ----     REST endpoints   ----
POST      /subscription/v1/subscribe     com.botocrypt.arbitrage.controllers.SubscriptionController.addSubscription()
# ---- end of REST endpoints ----
